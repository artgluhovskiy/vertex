/**
 * Constants for the directory tree component
 */

/** Height of a single tree item in pixels */
export const TREE_ITEM_HEIGHT = 32;

/** Indentation increment per level in pixels */
export const TREE_INDENT_INCREMENT = 16;

/** Maximum height for the collapsible section in pixels */
export const SECTION_MAX_HEIGHT = 1000;

/** Offset for the chevron icon center position in pixels */
export const CHEVRON_CENTER_OFFSET = 10;

/** Base padding for tree items in pixels */
export const TREE_ITEM_BASE_PADDING = 4;
